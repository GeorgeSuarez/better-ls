# Better LS

A simple CLI tool that outputs file paths in a better format all written in Rust.

## Install

Simple way to install is using Rust:

```bash
cargo install --path .

```

Then type in `bash better-ls` from where in the terminal.

## Usage

```bash
better-ls <PATH>

```

## Screen Shots
